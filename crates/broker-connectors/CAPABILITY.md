# Connector capability table (B1-05)

One row per sink module in `crates/broker-connectors/src` (every file
except `lib.rs`). Each row was derived from the module source and its
`#[cfg(test)]` tests. No row claims a live-server run: every test in
this crate passes offline with `cargo test -p broker-connectors`.

Method:

- `Speaks` is what the sink actually puts on the wire today: `native`
  wire protocol over TCP, `vendor HTTP API` via `reqwest`, `line`
  protocol, `local file`, or `in-memory only`.
- `Tested against` is what the proving test actually runs against:
  `mock transport` (in-memory `Mock*`/`Memory*`), or `loopback fake`
  (an in-process `TcpListener` on `127.0.0.1:0` or ephemeral `axum`
  server scripted for one exchange). Neither is the vendor's real
  server.
- `Proving test` names one test by path. All paths are
  `crates/broker-connectors/src/<module>.rs`.

Dependency note: `crates/broker-connectors/Cargo.toml` pulls in the
maintained vendor drivers `clickhouse 0.15` for the clickhouse sink only
(`DriverClickHouseTransport` with driver-owned HTTP pool, Basic auth
and `RowBinaryWithNamesAndTypes` inserts),
`tokio-postgres 0.7` +
`tokio-postgres-rustls` for the cockroachdb sink only
(`PgDriverCockroachDbTransport` with `rustls` system roots),
`aws-sdk-dynamodb 1` for the dynamodb sink only
(`SdkDynamoDbTransport` with driver-owned SigV4),
`elasticsearch 8` (+ `http 1` for per-request headers) for the
elasticsearch sink only (`DriverElasticsearchTransport` sending the
sink-rendered bulk NDJSON through the official client),
`couchbase 1` for the couchbase sink only
(`DriverCouchbaseTransport` with upsert/insert/replace plus
scope/collection management),
`google-cloud-pubsub 1` (+ `google-cloud-gax 1` for status-code mapping
in tests) for the gcp_pubsub sink only (`SdkGcpPubSubTransport`
publishing base64 data with ordering keys and attributes through the
official gRPC `Publisher`), and
`rdkafka 0.39` (SASL_SSL + SASL) plus `schema-registry-client 0.4`
for the confluent sink only (`RdkafkaConfluentTransport` with the
system OpenSSL roots, `RegistrySchemaClient` for Avro/JSON subjects;
`apache-avro 0.15` proves Avro framing in dev-tests); `rdkafka 0.39`
for the kafka sink (`RdkafkaKafkaTransport`, plaintext + acks +
idempotence where configured; `FutureProducer` delivery futures); no other
sink uses a maintained vendor driver (`mysql_async`,
`redis`, `mongodb`, other cloud SDKs remain absent). Every other TCP sink is
hand-written over `tokio::net::TcpStream`; the AWS IoT and Azure IoT
sinks upgrade that socket with `tokio-rustls`/`rustls` (platform roots
plus a configured CA bundle); every cloud sink is otherwise
hand-written over `reqwest`. The `dev-dependencies` are `axum` for
the loopback fakes and `apache-avro` for the confluent Avro vectors.

Counts: 47 sink modules. 19 speak a native wire protocol over TCP
(alloydb, aws_iot, azure_iot, cassandra, cockroachdb, confluent, couchbase,
gcp_iot, kafka, mongodb, mqtt_bridge, mssql, mysql, opc_ua, postgres,
pulsar, rabbitmq, redis, timescaledb). 0 have been run against a real
vendor server in-tree. 47 have only mock or loopback-fake coverage.

| Sink | Speaks | Tested against | Proving test |
|------|--------|----------------|--------------|
| alloydb | native PG-compatible wire over TCP (`TcpAlloydbTransport`, startup + cleartext password, Simple Query) | mock transport | `crates/broker-connectors/src/alloydb.rs:1120 test_alloydb_sink_loopback_success` (`MockAlloydbTransport`; no `TcpListener`) |
| aws_iot | native MQTT over TLS with mutual X.509 (`TlsAwsIotTransport` via `mqtt_bridge::encode_publish` + CONNECT/CONNACK; SigV4 URL signer, CA bundle + ALPN `mqtt`, connect/handshake timeouts) | loopback TLS fake (in-process `TcpListener` + `tokio-rustls` server requiring client cert, CONNECT/CONNACK/PUBLISH; plus `MockAwsIotTransport`) | `crates/broker-connectors/src/aws_iot.rs:test_tls_handshake_and_publish` |
| azure_blob | vendor HTTP API (`HttpAzureBlobTransport` PUT block-blob with `x-ms-blob-type`, `x-ms-version`, SharedKey/SAS/Bearer) | loopback fake (ephemeral `axum` PUT capture; plus `MockAzureBlobTransport`) | `crates/broker-connectors/src/azure_blob.rs:1042 test_loopback_put_headers` |
| azure_eventhubs | vendor HTTP API (`HttpAzureEventHubsTransport` POST `...servicebus.windows.net/.../messages` with SAS token) | mock transport | `crates/broker-connectors/src/azure_eventhubs.rs:851 test_batch_framing_and_partition_routing` (`MockAzureEventHubsTransport`; no `TcpListener`) |
| azure_iot | native MQTT over TLS (`TlsAzureIotTransport` D2C/twin topics via `mqtt_bridge::encode_publish` + CONNECT/CONNACK; SAS as MQTT password with configurable TTL or X.509 client cert, CA bundle, connect/handshake timeouts) | loopback TLS fake (in-process `TcpListener` + `tokio-rustls` server for SAS and X.509 paths; plus `MockAzureIotTransport`) | `crates/broker-connectors/src/azure_iot.rs:test_tls_handshake_with_sas_password` |
| bigquery | vendor HTTP API (`HttpBigQueryTransport` POST `insertAll` with Bearer token) | mock transport | `crates/broker-connectors/src/bigquery.rs:990 test_insert_flow_and_ids` (`MockBigQueryTransport`; no `TcpListener`) |
| cassandra | native CQL binary v4 over TCP (`NativeCassandraTransport`, STARTUP/AUTH/USE/UNLOGGED BATCH, password auth) | loopback fake (in-process `TcpListener` scripting startup/auth/batch; plus `MockCassandraTransport`) | `crates/broker-connectors/src/cassandra.rs:1498 test_tcp_loopback_startup_auth_use_batch` |
| clickhouse | analytical rows over the maintained `clickhouse` driver (`DriverClickHouseTransport` inserting `RowBinaryWithNamesAndTypes` with Basic auth and database; legacy hand-written `HttpClickHouseTransport` POST `?query=INSERT ... FORMAT JSONEachRow` retained for offline unit tests only) | loopback fake for unit tests (ephemeral `axum` capture via `serve_captured`; plus `MockClickHouseTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `CLICKHOUSE_*` env) | `crates/broker-connectors/src/clickhouse.rs` `test_batch_posts_json_each_row` + `test_qualify_driver_write_path` |
| cockroachdb | PG-compatible wire via the maintained `tokio-postgres` driver with `rustls` TLS (`PgDriverCockroachDbTransport`; UPSERT/ON CONFLICT, retry on 40001). Legacy hand-written `TcpCockroachDbTransport` retained for offline unit tests only | mock transport for unit tests (despite `loopback` in name, uses `MockCockroachDbTransport`; no `TcpListener`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `COCKROACHDB_*` env) | `crates/broker-connectors/src/cockroachdb.rs:1179 test_cockroach_sink_loopback_success` + `test_qualify_driver_write_path` |
| confluent | Kafka wire plus SASL/SCRAM over TLS via the maintained `rdkafka` driver (`RdkafkaConfluentTransport`, SASL_SSL, acks=all; Schema Registry via `schema-registry-client`, `{topic}-value` subjects, magic `0x00` + schema-id framing). Legacy hand-written `TcpConfluentTransport` (ApiVersions + SASL PLAIN/SCRAM + Produce v3 RecordBatch-v2) retained for offline unit tests only | loopback fake + mock transport for unit tests (in-process fake broker `TcpListener`; plus `MemoryConfluentTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `CONFLUENT_*` env) | `crates/broker-connectors/src/confluent.rs:1446 test_tcp_plain_produce_against_fake_broker` + `test_qualify_driver_write_path` |
| couchbase | KV via the maintained `couchbase` driver (`DriverCouchbaseTransport`, upsert/insert/replace with scope/collection + expiry). Legacy hand-written `NativeCouchbaseTransport` (SET/ADD/REPLACE opcodes + SASL PLAIN, pipelined opaque) retained for offline unit tests only | loopback fake for unit tests (in-process `TcpListener` verifying SASL PLAIN credentials + batch, AUTH_ERROR fail-closed; plus `MockCouchbaseTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `COUCHBASE_*` env) | `crates/broker-connectors/src/couchbase.rs` `test_tcp_loopback_sasl_and_pipelined_batch` + `test_qualify_driver_write_path` |
| databricks | vendor HTTP API (`HttpDatabricksTransport` POST `/api/2.0/sql/statements` with Bearer over the maintained `reqwest` driver, `GET .../statements/{id}` polling to `SUCCEEDED`, rotatable Bearer) | loopback fake (ephemeral `axum` Statements API: PENDING -> RUNNING -> SUCCEEDED polls, 401 fail-closed, Bearer renewal; plus `MockDatabricksTransport`) for unit tests; real-workspace qualification in `test_qualify_statements_write_path` (ignored, needs `DATABRICKS_*` env) | `crates/broker-connectors/src/databricks.rs` `test_http_polling_reaches_succeeded_through_manager` + `test_qualify_statements_write_path` |
| datalayers | vendor HTTP API (`HttpDatalayersTransport` POST `/api/v1/write?db=` JSON with Bearer over the maintained `reqwest` driver, microsecond timestamps; `POST /api/v1/sql` for create/query-back) | loopback fake (ephemeral `axum` write capture: Bearer + JSON + precision + 401 fail-closed; plus `MockDatalayersTransport`) for unit tests; real-server qualification in `test_qualify_write_path` (ignored, needs `DATALAYERS_*` env) | `crates/broker-connectors/src/datalayers.rs` `test_http_write_posts_bearer_and_json_through_manager` + `test_qualify_write_path` |
| disk_log | local file (append/rotate/gzip/retention via `tokio::fs`; `FileDiskLogWriter` real, `MemoryDiskLogWriter` virtual) | real local directory (temp dir via `tokio::fs`; 5000-row rotating gzip log, simulated kill mid-write with torn-tail truncation, retention cap, gunzip diff) | `crates/broker-connectors/src/disk_log.rs` `test_ndjson_formatting` + `test_qualify_file_write_path_crash_recovery` |
| doris | vendor HTTP API (`HttpDorisTransport` PUT `/_stream_load` with Basic auth, label, manual 307 handling over the maintained `reqwest` driver; DDL/query-back for qualification over the maintained `mysql_async` client) | loopback fake (ephemeral `axum` 307 with auth-replay assertion; plus `MockDorisTransport`) for unit tests; real-server qualification in `test_qualify_stream_load_write_path` (ignored, needs `DORIS_*` env) | `crates/broker-connectors/src/doris.rs` `test_307_redirect_loopback` + `test_qualify_stream_load_write_path` |
| dynamodb | vendor HTTP API on the maintained `aws-sdk-dynamodb` driver (`SdkDynamoDbTransport` `BatchWriteItem` with driver-owned SigV4; legacy hand-written `HttpDynamoDbTransport` retained for offline unit tests only) | mock transport for unit tests (`MockDynamoDbTransport`; no `TcpListener`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `DYNAMODB_*` env) | `crates/broker-connectors/src/dynamodb.rs` `test_unprocessed_requeues_selectively` + `test_qualify_driver_write_path` |
| elasticsearch | vendor HTTP API on the maintained official `elasticsearch` driver (`DriverElasticsearchTransport` POST `/_bulk` NDJSON with Basic/ApiKey, index templates; legacy hand-written `HttpElasticsearchTransport` retained for offline unit tests only) | mock transport for unit tests (`MockElasticsearchTransport`; no `TcpListener`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `ELASTICSEARCH_*` env): 2000 docs (10 keys x 200) through the manager path, `_count` plus exact per-key counts from `_source`; 429 backoff proved offline via mock (not against the real server); secured-cluster Basic/ApiKey and the legacy HTTP transport not exercised against the real server | `crates/broker-connectors/src/elasticsearch.rs:1003 test_bulk_framing` + `test_qualify_driver_write_path` |
| gcp_iot | client for the Cloud IoT MQTT device protocol, which compatible endpoints still speak (MQTT 3.1.1, device JWT RS256/ES256 as password with username ignored, full device path as client id, telemetry/state topics; proactive refresh), over `tokio-rustls` with system roots plus optional CA bundle: TLS by default, plaintext only for an explicit `mqtt://` endpoint (logged warning); no `rumqttc` dependency; legacy hand-written `TcpGcpIotTransport` retained for offline unit tests only. Google's own Cloud IoT service is retired, so there is no vendor-server run: qualification is against a compatible witness broker that verifies each device JWT and refuses expired, forged and anonymous connections | loopback TLS fakes verifying the received JWT against the fixture public key (in-process `TcpListener` + `tokio-rustls` server asserting CONNECT/CONNACK/PUBLISH; plus `MockGcpIotTransport`) for unit tests; witness qualification in `test_qualify_tls_write_path` (ignored, needs `GCP_IOT_*` env) | `crates/broker-connectors/src/gcp_iot.rs:test_tls_handshake_connect_and_publish_qos0` + `test_qualify_tls_write_path` |
| gcp_pubsub | vendor gRPC API on the maintained `google-cloud-pubsub` driver (`SdkGcpPubSubTransport` publish with base64 data, ordering keys, attributes; legacy hand-written `HttpGcpPubSubTransport` POST `:publish` retained for offline unit tests only) | mock transport + loopback fake for token cache for unit tests (`MockGcpPubSubTransport`; ephemeral `axum` `/token`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `PUBSUB_*` env) | `crates/broker-connectors/src/gcp_pubsub.rs` `test_publish_framing` + `test_qualify_driver_write_path` |
| greptimedb | vendor HTTP API + line protocol over HTTP (`HttpGreptimeDbTransport` POST `/sql` form and `/influxdb/api/v2/write` text) | mock transport (despite `loopback` in name, uses `MockGreptimeDbTransport`; no `TcpListener`) | `crates/broker-connectors/src/greptimedb.rs:918 test_greptime_sink_sql_loopback` |
| http | generic vendor HTTP API (webhook via `ReqwestHttpTransport`, POST/PUT/PATCH, Basic/Bearer/ApiKey + HMAC) | mock transport (`MockHttpTransport`; dummy `127.0.0.1:1` never bound) | `crates/broker-connectors/src/http.rs:1101 test_batch_array_wrapping` |
| influxdb | line protocol over HTTP, vendor HTTP API (`reqwest` POST `/api/v2/write?org=&bucket=` text/plain, Token auth; no transport trait) | loopback fake (ephemeral `axum` capture via `serve_captured`) | `crates/broker-connectors/src/influxdb.rs:443 test_batch_posts_line_protocol` |
| iotdb | vendor HTTP API (`HttpIotDbTransport` POST `/insertTablet` JSON, Basic auth) | mock transport | `crates/broker-connectors/src/iotdb.rs:927 test_tablet_grouping_and_types` (`MockIotDbTransport`; no `TcpListener`) |
| kafka | native Kafka wire via the maintained `rdkafka` driver (`RdkafkaKafkaTransport`, RecordBatch v2, murmur2, acks, idempotence where configured). Legacy hand-written `TcpKafkaTransport` (Produce v3/ApiVersions over TCP, acks only) retained for offline unit tests only | loopback fake + mock transport for unit tests (in-process fake broker `TcpListener`; plus `MemoryKafkaTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `KAFKA_*` env) | `crates/broker-connectors/src/kafka.rs:985 test_tcp_transport_produce_against_fake_broker` + `test_qualify_driver_write_path` |
| kinesis | vendor HTTP API (`HttpKinesisTransport` POST `PutRecords` JSON with SigV4) | mock transport | `crates/broker-connectors/src/kinesis.rs:996 test_partial_failure_retries_only_failed` (`MockKinesisTransport`; no `TcpListener`) |
| mongodb | native OP_MSG over TCP with SCRAM-SHA-256 (`NativeMongoDbTransport`, pbkdf2 + HMAC) | mock transport (grouping only; no `TcpListener`) | `crates/broker-connectors/src/mongodb.rs:1718 test_grouping_by_collection` |
| mqtt_bridge | MQTT 3.1.1/5.0 outbound bridge via the maintained `rumqttc` driver (`RumqttcMqttBridgeTransport`, CONNECT auth, driver packet-id tracking, inflight cap; plaintext TCP only, TLS rejected). Legacy hand-written `TcpMqttBridgeTransport` (CONNECT/CONNACK/PUBLISH framing) retained for offline unit tests only | loopback fake + mock transport for unit tests (in-process `TcpListener` connect + publish; plus `MemoryMqttBridgeTransport`); real-server qualification in `test_qualify_bridge_write_path` (ignored, needs `MQTT_BRIDGE_*` env) | `crates/broker-connectors/src/mqtt_bridge.rs:1069 test_tcp_loopback_connect_and_publish` + `test_qualify_bridge_write_path` |
| mssql | native TDS over TCP (`NativeMssqlTransport`, PRELOGIN + LOGIN7 + `sp_executesql`, typed params) | loopback fake (in-process `TcpListener` scripting prelogin/login/batch) | `crates/broker-connectors/src/mssql.rs:1370 test_tcp_loopback_prelogin_login_batch` |
| mysql | native MySQL client protocol via the maintained `mysql_async` driver (`DriverMySqlTransport`, handshake + mysql_native_password/caching_sha2_password, COM_STMT_PREPARE/EXECUTE with bound params). Legacy hand-written `TcpMySqlTransport` (handshake + `mysql_native_password`, COM_STMT_PREPARE/EXECUTE) retained for offline unit tests only | loopback fake + mock transport for unit tests (in-process `TcpListener` scripting handshake/prepare/batch; plus `MemoryMySqlTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `MYSQL_*` env) | `crates/broker-connectors/src/mysql.rs:1192 test_tcp_handshake_prepare_and_batch_params` + `test_qualify_driver_write_path` |
| oci_streaming | vendor HTTP API (`HttpOciStreamingTransport` POST `/20180418/streams/.../messages` with OCI RSA-SHA256 signature) | loopback fake (ephemeral `axum` signed-PUT capture) | `crates/broker-connectors/src/oci_streaming.rs:1188 test_loopback_signed_put` |
| opc_ua | native OPC-UA TCP framing (`TcpOpcUaTransport`, HEL/ACK + OPN/CLO chunks, Variant/DataValue codec; Anonymous/Username/Certificate) | loopback fake (in-process `TcpListener` hello/ack + datachange) | `crates/broker-connectors/src/opc_ua.rs:1640 test_loopback_hello_ack_and_datachange` |
| opentsdb | vendor HTTP API; telnet line protocol stubbed (`NetworkOpenTsdbTransport::put_http` POST `/api/put`; `put_telnet` returns terminal error, `serialize_telnet_lines` only formats) | mock transport (despite `loopback` in name, uses `MockOpenTsdbTransport`; no `TcpListener`) | `crates/broker-connectors/src/opentsdb.rs:762 test_opentsdb_sink_http_loopback` |
| oracle | vendor HTTP API, not TNS: POSTs JSON `{statementText, binds}` (MERGE ... USING DUAL) to Oracle REST Data Services with Basic auth (`HttpOracleTransport`). The module name implies the Oracle wire protocol; it does not speak TNS. | mock transport (despite `loopback` in name, uses `MockOracleTransport`; no `TcpListener`) | `crates/broker-connectors/src/oracle.rs:696 test_oracle_sink_loopback_success` |
| postgres | native PG extended protocol via the maintained `tokio-postgres` driver with `rustls` (`DriverPgTransport`, SCRAM-SHA-256/MD5 via the driver, bound `$1/$2/$3` params, driver-client pool). Legacy hand-written `TcpPgTransport` (SSLRequest + startup + MD5/SCRAM-SHA-256, Parse/Bind/Describe/Execute/Sync) retained for offline unit tests only | loopback fake + mock transport for unit tests (in-process `TcpListener` scripting MD5/SCRAM + batch; plus `MemoryPgTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `POSTGRES_*` env) | `crates/broker-connectors/src/postgres.rs:1281 test_tcp_md5_auth_and_batch_params` + `test_qualify_driver_write_path` |
| pulsar | native Pulsar binary over TCP (framing + CRC32C + Connect/Producer/Send, JWT or none via `TcpPulsarTransport`) | loopback fake (in-process `TcpListener` handshake + send) | `crates/broker-connectors/src/pulsar.rs:1490 test_tcp_loopback_handshake_and_send` |
| rabbitmq | native AMQP 0-9-1 over TCP (`TcpRabbitTransport`, PLAIN handshake + Basic.Publish + content header/body frames) | loopback fake (in-process `TcpListener` handshake + publish; plus `MemoryAmqpTransport`) | `crates/broker-connectors/src/rabbitmq.rs:699 test_tcp_transport_handshake_and_publish` |
| redis | native RESP over TCP (`TcpRedisTransport`, AUTH + SELECT + pipelined commands) | loopback fake (in-process `TcpListener` with canned replies; plus `MemoryRedisTransport`) | `crates/broker-connectors/src/redis.rs:742 test_tcp_auth_select_and_pipelining` |
| redshift | vendor HTTP API (`HttpRedshiftTransport` POST `BatchExecuteStatement` with SigV4) | mock transport | `crates/broker-connectors/src/redshift.rs:948 test_batch_flow_and_targeting` (`MockRedshiftTransport`; no `TcpListener`) |
| rocketmq | custom length-prefixed JSON envelope over TCP (`TcpRocketMqTransport`, `[0x01][u32 len][JSON SendMessage]`, HMAC-SHA1). This is not the vendor Remoting/gRPC protocol; the module name implies RocketMQ wire compatibility it does not have. | loopback fake (in-process `TcpListener` envelope check + `{"code":0}` ack) | `crates/broker-connectors/src/rocketmq.rs:1135 test_tcp_loopback_envelope` |
| s3 | vendor HTTP API (`HttpS3Transport` PUT object ndjson/gzip with SigV4) | mock transport (framing only; no `TcpListener`) | `crates/broker-connectors/src/s3.rs:766 test_ndjson_framing` |
| s3_tables | vendor HTTP API (`HttpS3TablesTransport` PUT data + JSON snapshot with SigV4) | loopback fake (ephemeral `axum` signed-PUT capture) | `crates/broker-connectors/src/s3_tables.rs:1197 test_loopback_put_signed` |
| snowflake | vendor HTTP API (`HttpSnowflakeTransport` POST streaming `/rows` with RS256 JWT Bearer) | mock transport | `crates/broker-connectors/src/snowflake.rs:900 test_row_building_and_uppercase_tables` (`MockSnowflakeTransport`; no `TcpListener`) |
| sparkplug_b | in-memory only: clean-room Tahu Protobuf codec + state machine (`MemorySparkplugTransport::publish`); no `TcpStream`, no `reqwest`, no file transport | mock transport (codec vectors + memory publish only) | `crates/broker-connectors/src/sparkplug_b.rs:1724 test_sink_encodes_normalized_documents` |
| tablestore | vendor HTTP API (`HttpTablestoreTransport` POST `BatchWriteRow` with HMAC-SHA1 + MD5) | loopback fake (ephemeral `axum` `/api/BatchWriteRow` capture) | `crates/broker-connectors/src/tablestore.rs:1220 test_loopback_headers_and_body` |
| tdengine | vendor HTTP API (`HttpTdengineTransport` POST SQL `INSERT INTO ... USING ... TAGS...` with Basic/Taosd auth) | mock transport (rendering only; no `TcpListener`) | `crates/broker-connectors/src/tdengine.rs:895 test_insert_rendering` |
| timescaledb | hypertable DDL plus PG wire via the maintained `tokio-postgres` driver with `rustls` TLS (`DriverTimescaleDbTransport`; UPSERT/ON CONFLICT, bound `$1..$4`, retry on 08/57P/40001/40P01). Legacy hand-written `TcpTimescaleTransport` retained for offline unit tests only | mock transport for unit tests (plus `MockTimescaleTransport`); real-server qualification in `test_qualify_driver_write_path` (ignored, needs `TIMESCALEDB_*` env) | `crates/broker-connectors/src/timescaledb.rs test_hypertable_parameter_binding` + `test_qualify_driver_write_path` |
| timestream | vendor HTTP API (`HttpTimestreamTransport` POST `WriteRecords` with SigV4) | mock transport | `crates/broker-connectors/src/timestream.rs:1163 test_partial_rejection_requeues_selectively` (`MockTimestreamTransport`; no `TcpListener`) |
